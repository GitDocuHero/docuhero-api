steps:
  # Step 1: Run database migrations
  - name: 'node:20'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        npm install
        npx prisma generate
        npx prisma migrate deploy
    secretEnv: ['DATABASE_URL']

  # Step 2: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/hero-api:$COMMIT_SHA', '.']

  # Step 3: Push Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/hero-api:$COMMIT_SHA']

  # Step 4: Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'hero-api'
      - '--image'
      - 'gcr.io/$PROJECT_ID/hero-api:$COMMIT_SHA'
      - '--region'
      - 'us-east1'
      - '--platform'
      - 'managed'
      - '--add-cloudsql-instances'
      - 'docuhero-583a5:us-east1:docuhero-db'
      - '--update-secrets'
      - 'DATABASE_URL=DATABASE_URL:latest,FIREBASE_KEY=FIREBASE_KEY:latest,FIREBASE_WEBAPP_CONFIG=FIREBASE_WEBAPP_CONFIG:latest,GEMINI_KEY=GEMINI_KEY:latest'
      - '--set-env-vars'
      - 'NODE_ENV=production,PORT=8080'
      - '--allow-unauthenticated'

options:
  logging: CLOUD_LOGGING_ONLY

availableSecrets:
  secretManager:
    - versionName: projects/docuhero-583a5/secrets/DATABASE_URL/versions/latest
      env: 'DATABASE_URL'
    - versionName: projects/docuhero-583a5/secrets/FIREBASE_KEY/versions/latest
      env: 'FIREBASE_KEY'
    - versionName: projects/docuhero-583a5/secrets/FIREBASE_WEBAPP_CONFIG/versions/latest
      env: 'FIREBASE_WEBAPP_CONFIG'
    - versionName: projects/docuhero-583a5/secrets/GEMINI_KEY/versions/latest
      env: 'GEMINI_KEY'

images:
  - 'gcr.io/$PROJECT_ID/hero-api:$COMMIT_SHA'
